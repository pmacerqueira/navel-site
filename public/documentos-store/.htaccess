# Bloquear acesso HTTP directo aos ficheiros; só o PHP (documentos-api.php) serve downloads autenticados.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
