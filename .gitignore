# Dependencies
node_modules/

# Build output
dist/
navel-publicar.zip

# Catálogos (PDFs e thumbnails — já estão no cPanel, não precisam de estar no repo)
public/catalogos/
public/images/catalogos/

# Backups (evitar ficheiros grandes no repo)
*.tar.gz
backup-*.tar.gz

# Vite cache
.vite/

# Environment
.env
.env.local
.env.*.local
.env.cpanel
.env.cpanel.local
Supabase e MySQL.txt
# Chave anon só para keep-alive PHP (não commitar)
public/keep-alive-supabase.secret.php
public/documentos-api-config.php
docs/CREDENCIAIS.md

# Permissões reais da API de documentos (contém emails / listas de utilizadores).
# O ficheiro `.example` fica versionado como template; o real vive só no servidor.
public/documentos-store/.navel-permissions.json

# Cache do deploy automático cPanel (scripts/cpanel-deploy.mjs)
scripts/.cpanel-deploy-cache.json

# Comandos de cron gerados localmente (podem conter token OneDrive)
scripts/.cpanel-cron-paste.txt

# Logs
*.log
npm-debug.log*

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
